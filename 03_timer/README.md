# Timers and interrupts

The simple timer + interrupt demonstration. Using timer and interrupt service routine to toggle 3 LEDs.

## Usage

The standard procedure:

```
make all
make flash
```

To see how to connect LEDS, check the image in 04_weather example, just ignore DHT11 sensor.
